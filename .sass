#!/usr/bin/env bash
# Emergency CI shim for hardcoded sass calls.
# Ensures css/index.css exists by compiling via builder or copying fallback.
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# Pre-ensure CSS via robust builder
bash "${ROOT_DIR}/scripts/build-css.sh" || true
# If still missing, copy fallback
if [ ! -f "${ROOT_DIR}/css/index.css" ] && [ -f "${ROOT_DIR}/css/index.fallback.css" ]; then
  cp -f "${ROOT_DIR}/css/index.fallback.css" "${ROOT_DIR}/css/index.css"
fi
echo "[.sass] Ensured css/index.css is present."
exit 0
